

<div class="container mb-5">
    <div class="row">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">List of Names using @@Ref Attribute</div>
                <div class="card-body">
                    <ol class="list-group">
                       @*  @if (Names!.Count > 0)
                        {
                            foreach (string name in Names)
                            {
                                <li class="list-group-item">@name</li>
                            }
                        } *@
                        @if (Users!.Count > 0)
                        {
                            foreach (User user in Users)
                            {
                                <li class="list-group-item">@user.Id @user.Name  @user.Age</li>
                            }
                        }
                    </ol>
                </div>
            </div>
        </div>
    </div>
</div>

@code{
   // private List<string>? Names { get; set; } = new();
    private List<User>? Users { get; set; } = new();

    // public void AddName(string name)
    // {
    //     if (string.IsNullOrEmpty(name) || string.IsNullOrWhiteSpace(name)) return;
    //     Names!.Add(name);
    //     StateHasChanged();
    // } 
    
    public void AddUser(User user)
    {
        if (user is null) return;
        Users!.Add(user);
        StateHasChanged();
    }
}